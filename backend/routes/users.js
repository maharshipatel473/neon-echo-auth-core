
const express = require('express');
const router = express.Router();
const { protect } = require('../middleware/auth');

// Get user profile, update user, etc. can be added here

module.exports = router;
